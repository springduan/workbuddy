# 2026-05-14 工作日志

## WorkBuddy 上线前代码修复

完成5项上线前必须修复的问题：

1. **🔴 NotifyConfirmModal.tsx API 地址硬编码修复**：将第105行硬编码的 `http://192.168.2.38:3000/api/notify` 改为使用环境变量 `import.meta.env.VITE_API_URL`，降级兜底 `http://localhost:3000`
2. **🔴 创建 .env.production**：新增 `app/.env.production`，设置 `VITE_API_URL=https://bybus.asia`，配合已有的 `.env.local` 开发环境
3. **🟡 CORS 收紧**：`server/index.js` 从 `app.use(cors())` 改为只允许 `https://bybus.asia`、`http://localhost:5173`、`http://localhost:3000` 三个来源
4. **🟡 JWT_SECRET 安全强化**：在 `server/.env` 中新增 64 位随机密钥，同时 `server/index.js` 的默认值从硬编码字符串改为运行时生成随机密钥 + 警告日志
5. **🟡 延误弹窗文案修正**：将"提交后不可二次修改"改为"如需修改，可先恢复为正常状态后重新登记"，与实际可恢复逻辑一致
