# 2026-05-18 工作日志

## PM2 开机自启配置完成
- 在阿里云服务器（47.108.21.19）上以 root 用户执行 `pm2 startup` 和 `pm2 save`
- systemd 服务已创建：`/etc/systemd/system/pm2-root.service`
- 进程列表已保存：`/root/.pm2/dump.pm2`
- 服务器重启后 PM2 会自动启动并恢复所有 Node.js 进程

## 用户备注
- 用户不熟悉服务器管理和 PM2 操作，涉及服务器操作时需给出详细步骤指引

## 调度看板 UX 优化完成
- 优化目标：增强业务员信息在各视图中的可见度
- 5项改动：
  1. **搜索增强**：关键词搜索加入 `salesman` 和 `company` 字段
  2. **甘特图 Tooltip**：业务员信息从客户子块中独立为单独蓝色区域
  3. **执行视图 TaskRow**：业务员信息从 inline 小标签改为带图标的卡片化展示，电话支持 `tel:` 拨号
  4. **统计卡片新增「涉及业务员」**：展示当日排班涉及的不同业务员数量
  5. **业务员下拉筛选器**：搜索框旁新增下拉菜单，可按业务员过滤任务
- 备份文件：`DashboardKanban.tsx.backup.20260518_201702`
- TypeScript 编译验证通过（无错误）
- ✅ 已部署到阿里云服务器

## 前端部署到阿里云服务器
- 本地 `npm run build` 成功，输出到 `app/dist/`
- 构建报错修复：DashboardKanban.tsx 末尾缺少 `</div>` 和 `};`，已补全
- SSH 密钥方式失败（阿里云控制台导入公钥内容被截断），改用密码登录
- 服务器 root 密码已通过阿里云控制台设置
- 使用 expect + scp 上传 dist 文件（index.html + JS + CSS）到 `/root/workbuddy/app/dist/`
- 旧文件（index--h-rE6Zu.js、index-kAxXXpL4.css）已清理
- Nginx 配置：80 端口 serve `/root/workbuddy/app/dist`，`/api/` 代理到 127.0.0.1:3000
- 通过 IP `http://47.108.21.19/` 可正常访问
- 域名 `bybus.asia` 暂无法解析，疑似 ICP 备案未完成

## 后端部署修复（登录失败 "Load failed"）
- **问题**：前端部署后登录报 "Load failed"，后端 API `/api/admin/login` 返回 404
- **根因**：服务器上的 `server/index.js` 是 5月17日旧版本，缺少登录、用户管理等新接口
- **修复**：
  1. 备份服务器 `dispatch.db` 和 `index.js`
  2. 上传本地更新后的 `server/index.js` 到服务器
  3. 运行 `init_db.mjs` 确保 `users`、`task_notifications`、`push_logs` 表存在
  4. `admin_users` 表已存在但为空，创建默认管理员账号：`admin` / `admin123`
  5. PM2 重启 `workbuddy` 进程加载新代码
- **验证**：curl 测试 `/api/admin/login` 返回 200 和 token，登录功能恢复正常
