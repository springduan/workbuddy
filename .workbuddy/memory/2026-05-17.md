# 2026-05-17 工作日志

## 服务器环境搭建进度

在阿里云服务器（IP: 47.108.21.19，Ubuntu 22.04）上进行排班系统部署准备：

### 已完成
1. ✅ 设置 root 密码
2. ✅ 系统更新（apt update && apt upgrade -y）
3. ✅ 安装 Node.js 20.20.2（通过 Nodesource）
4. ✅ 本地打包项目（排除 node_modules/dist/.git/.env），生成 workbuddy-deploy.tar.gz（197KB）放在桌面
5. ✅ 编写完整部署指南（workbuddy-deploy-guide.md），12步从上传到验证

### 已完成
6. ✅ 通过 ghfast.top 镜像克隆项目到 /root/workbuddy
7. ✅ 配置 server/.env 环境变量（微信小程序 + JWT）
8. ✅ npm install（server 118包 / app 493包）+ 构建前端（dist/ 生成成功）
9. ✅ Nginx 安装配置（80端口前端静态文件 + /api/ 反向代理3000）
10. ✅ PM2 启动后端服务（workbuddy 进程 online，API 返回"服务运行正常"）
11. ✅ 验证通过：http://47.108.21.19 可访问（ICP备案期间用IP）

### 待完成
12. ⬜ PM2 开机自启（pm2 startup）
13. ⬜ ICP 备案通过后配置 SSL（certbot）+ 切换到 bybus.asia 域名
14. ⬜ 登录测试（管理员账号功能验证）

### 部署中修复的 Bug
- `init_db.mjs`：`UNIQUE(task_id, openid)` 约束位置错误（放在列定义中间而非末尾），导致 SQLite 语法报错 `near "task_date": syntax error`，已将 UNIQUE 移到 sent_at 列之后

### 备注
- ICP 备案在管局审核中（第5步），预估11个工作日
- 备案期间可用 IP 地址直接访问测试
- 用户对 Linux 不熟悉，需要逐步给命令复制粘贴执行
- GitHub 从服务器无法直接克隆（网络限制），镜像站也失败，改用本地打包上传方案
- 本机没有 SSH 密钥无法直接 scp，需通过阿里云控制台 Web 终端或手动上传
