
## 导入去重功能（2026-05-21）
- `useAppData.ts` - `importCustomers`：以姓名+手机号为 key 去重，同时对 Excel 文件内部行去重
- `useAppData.ts` - `importVehicles`：以车牌号为 key 去重
- 返回值从数组改为 `{ added, skippedCount }`
- `Dashboard.tsx` - 导入成功提示中新增跳过条数展示
